>>>> git init

>>>> git config user.name "name" or git config --global user.name "name"

>>> git config user.email "email_id"

>>> git add .  or git add <file name>

>>> git commit -m "commit message"

>>> git add .; git commit -m "commit message"

>>> git status

>>> git log

>>> git log --name-only

>>> git log --oneline

To see the content what we have written.

>>> git show commit id

To see the commit those I have forget

>>> git log --grep "commit message"

To see the latest commit
>>>> git log -1(last commit)
>>>> git log -2 (last 2 commit)

Branch command

To see the branch
>>> git branch

To create branch

>>> git branch  <branch_name>

To checkout the branche

>>>>  git checkout <branch_name>

To create and checkout the branch

>>> git checkout -b <branch_name>

To delete the branch

>>> git branch -d <branch>
    git branch -D <branch>

Git stash commnads

>>>> git stash
>>>> git stash list>>>> git stash apply stash{0} <which number you want apply>
>>>> git stash clear

Git reset commands

>>>> git reset .
     git reset --hard

To clean the untrack file

>>>> git clean -f
     git clean -n
Git tag

>>>> git tag -a <tag_name> -m "commit message" commitID
     git show <tag name>
     git tag -d <tag name>

>>>> git clone <url of git repo>

Revert command

>>>>> git revert commitID


